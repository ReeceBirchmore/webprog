{
  "name": "webprog",
  "version": "1.0.0",
  "description": "Web Programming Coursework",
  "main": "svr.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "cd dbabstract && node setup",
    "create": "createdb quiz && psql quiz < init.sql",
    "start": "node svr"
  },
  "author": "UP850149",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^8.0.2"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "eslint-config-portsoc": "^0.6.0"
  }
}
